<!DOCTYPE html>
<html lang="ru">
<head>
    <title>Lab10 - RSA</title>
    <meta charset="utf-8"/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container">
    <ul class="nav justify-content-center mt-4">
        <li class="nav-item">
            <a class="nav-link active" href="/rsa">RSA</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="/el-gamal">Эль-Гамаля</a>
        </li>
    </ul>
    <h2 class="text-center mt-4">RSA</h2>
    
    <form method="POST" class="mt-4">
        <div class="form-group">
            <label for="message">Введите сообщение:</label>
            <textarea class="form-control" id="message" name="message" rows="3"><%= originalText %></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Зашифровать</button>
    </form>

    <% if (encryptedData) { %>
        <h5 class="text-center mt-4">Приватный ключ</h5>
        <p><%= privateKey %></p>

        <h5 class="text-center mt-4">Публичный ключ</h5>
        <p><%= publicKey %></p>

        <h5 class="text-center mt-4">Зашифрованное сообщение (<%= encryptionTime %>мс)</h5>
        <p style="word-break: break-all;"><%= encryptedData %></p>

        <h5 class="text-center mt-4">Расшифрованное сообщение (<%= decryptionTime %>мс)</h5>
        <p><%= decryptedData %></p>
    <% } %>
</div>
</body>
</html>